<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- 溢出菜单图标颜色，可以自己设置成任意的颜色-->
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <!--不带动画的对话框style-->
    <style name="style_dialog_without_anim" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ExpandedTitleStyle" >
        <item name="android:textSize">30sp</item>
    </style>
    <style name="CollapsedTitleStyle" >
        <item name="android:textSize">18sp</item>
    </style>
    <style name="TablayoutTextSize20" >
        <item name="android:textSize">20sp</item>
    </style>
    <style name="TablayoutTextSize24" >
        <item name="android:textSize">24sp</item>
    </style>
    <style name="TablayoutTextSize30" >
        <item name="android:textSize">35sp</item>
    </style>
    <style name="TablayoutTextSize18" >
        <item name="android:textSize">18sp</item>
    </style>
</resources>
